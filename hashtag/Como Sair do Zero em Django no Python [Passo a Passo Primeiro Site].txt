documentação do django

anaconda

venv

'polls/' + '' (diferente de '' + '<int:id>' no tech with tim)

settings.TIME_ZONE = 'America/Recife' (ou é 'Americas/Recife')

on_delete=models.CASCADE significa que ao apagar a question, as choices também são apagadas