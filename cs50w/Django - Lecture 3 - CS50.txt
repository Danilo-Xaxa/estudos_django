settings.py is a important file

urls.py is like table of contents

one django project may have multiple django apps

settings -> INSTALLED_APPS -> adicionar o nome do novo app (abordagem diferente !!!!!!!!!!!)

the new app name will be always in the url bar

<str:nome>

return render(request, "hello/index.html")

templates folder